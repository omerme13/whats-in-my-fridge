class ListItem {
    constructor(id, name, quantity, label) {
        this.id = id,
        this.name = name;
        this.quantity = quantity;
        this.label = label;
    }
}

export default ListItem;